int main()
{
  // if variable will never change use final or const
  // final variable can be set only once
  // a constanct variable is compile time constant
  final name='Bob';
  // can't change the value of final variable
  // constants are used for variable that are required to be compile time constants
  const bar=100;
  var b= const [];

  return 0;
}